<section class="hero">
  <div class="hero-image">
    <img src="/images/hero.webp" alt="hero foto" />
  </div>
  <div class="hero-text">
    <h1>Dele-tøj på abonnement</h1>
    <p>Hos Another Closet har vi skabt et socialt og bæredygtigt klædeskab, hvor du for 149,-/mdr. kan låne 3 unikke stykker tøj.</p>
    <a href="#find-klaedeskab" class="cta">Find dit lokale klædeskab her</a>
  </div>
</section>

<style>
  .hero {
    width: 100%;
  }

  .hero-image {
    position: relative;
    width: 100%;
    height: 60%;
    /* aspect-ratio: 16 / 9; */
    overflow: hidden;
  }

  .hero-image img {
    width: 100%;
    object-fit: cover;
    display: block;
  }

  /* Tekst ovenpå billedet */
  .hero-text {
    position: absolute;
    right: 5%;
    top: 50%;
    transform: translateY(-50%);
    text-align: right;
    max-width: 40%;
    color: black;
    padding: 2rem;
  }

  .hero-text h1 {
    font-weight: normal;
    font-size: 3rem;
    margin-bottom: 2rem;
  }

  .hero-text p {
    margin-bottom: 2rem;
    font-size: 1.2rem;
  }
  /* .hero {
    display: grid;
    grid-template-columns: 1fr 1fr;
  }

  .hero-image {
    width: 100%;
    aspect-ratio: 4 / 3; 
    overflow: hidden;
  }

  .hero-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .hero-text {
    padding: 1rem 2rem;
    display: flex;
    flex-direction: column;
    justify-content: center; 
    align-items: flex-end;
    text-align: right;
  }

  .hero-text h1 {
    font-weight: normal;
    margin-bottom: 2rem;
  }

  .hero-text p {
    margin-bottom: 2rem;
  } */

  .hero-text .cta {
    font-family: "futura-pt", Arial, Helvetica, sans-serif;
    font-size: 1.2rem;
    color: black;
    text-decoration: none;
    transition: text-decoration 0.2s ease;
  }

  .hero-text .cta:hover {
    text-decoration: underline;
  }

  /* Mobil layout */
  @media (max-width: 768px) {
    .hero {
      grid-template-columns: 1fr;
    }

    .hero-text {
      position: static;
      transform: none;
      background: none;
      text-align: left;
      max-width: 100%;
      padding: 2rem 1.5rem;
    }

    .hero-text h1 {
      font-size: 2.25rem;
      text-align: left;
    }
  }
</style>
