<header>
  <nav class="nav-left">
    <div class="dropdown">
      <a href="#find-klaedeskab" class="nav-link">Find nærmeste klædeskab</a>
      <div class="dropdown-menu">
        <a href="/product-view">Nørrebro</a>
        <a href="/product-view">Aau København</a>
        <a href="/product-view">Lyngby</a>
        <a href="/product-view">Roskilde</a>
        <a href="/product-view">Ringkøbing</a>
        <a href="/product-view">Fredericia</a>
        <a href="/product-view">Ribe</a>
      </div>
    </div>
  </nav>

  <div class="center">
    <a href="/" class="logo">ANOTHER CLOSET</a>
  </div>

  <nav>
    <a href="#faq" class="nav-link">Faq</a>
    <a href="#bliv-medlem" class="nav-link">Bliv medlem</a>
    <a href="#login" class="nav-link">Log in</a>
  </nav>

  <div class="hamburger" id="hamburger">
    <span></span>
    <span></span>
    <span></span>
  </div>

  <div class="mobile-menu" id="mobileMenu">
    <div class="submenu-toggle" id="toggleDropdown">
      Find nærmeste klædeskab
    </div>
    <div class="submenu" id="submenu">
      <a href="/product-view">Nørrebro</a>
      <a href="/product-view">Aau København</a>
      <a href="/product-view">Lyngby</a>
      <a href="/product-view">Roskilde</a>
      <a href="/product-view">Ringkøbing</a>
      <a href="/product-view">Fredericia</a>
      <a href="/product-view">Ribe</a>
    </div>
    <a href="#faq" class="nav-link">Faq</a>
    <a href="#bliv-medlem" class="nav-link">Bliv medlem</a>
    <a href="#login" class="nav-link">Log in</a>
  </div>
</header>

<style>
  header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem 2rem;
    font-family: "futura-pt", sans-serif;
    position: sticky;
    z-index: 1000;
    transition: transform 0.3s ease;
    top: 0;
    background-color: white;
  }

  nav {
    display: flex;
    gap: 2rem;
    align-items: center;
  }

  .center {
    flex: 1;
    text-align: center;
  }

  .nav-link {
    text-decoration: none;
    color: black;
    font-weight: 400;
    font-size: 1.2rem;
    transition: text-decoration 0.2s ease;
  }

  .nav-link:hover {
    text-decoration: underline;
  }

  .logo {
    font-family: "CCMetcon Heavy", sans-serif;
    font-size: 2rem;
    letter-spacing: 0.05em;
    text-decoration: none;
    color: black;
  }

  /* Dropdown styling */

  .dropdown {
    position: relative;
    display: inline-block;
  }

  .dropdown-menu {
    display: none;
    position: absolute; /* Ændret fra fixed */
    /* top: 100%;
    left: 0;
    width: 100vw; */
    /* background: rgba(255, 255, 255, 0.8); */
    background-color: aqua;
    padding: 1rem 0;
    justify-content: space-evenly;
    z-index: 999;
  }

  /* Denne selector sørger for at dropdown-menuen kun vises, når linket 'Find nærmeste klædeskab' er hovered */
  .dropdown:hover .dropdown-menu {
    display: flex;
  }

  /* Links i dropdown */
  .dropdown-menu a {
    color: black;
    font-size: 1.2rem;
    text-decoration: none;
    white-space: nowrap;
  }

  .dropdown-menu a:hover {
    text-decoration: underline;
  }

  /* .dropdown {
    position: relative;
  }

  .dropdown-menu {
    display: none;
    position: absolute;
    top: 100%;
    left: 0;
    background: rgba(255, 255, 255, 0.9);
    padding: 1rem 2rem;
    border-radius: 0.5rem;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    white-space: nowrap;
    z-index: 10;
  }

  .dropdown:hover > .nav-link + .dropdown-menu {
    display: flex;
    gap: 2rem;
    flex-wrap: wrap;
  }

  /* Undermenu links */
  .dropdown-menu a {
    color: black;
    font-size: 1.2rem;
    text-decoration: none;
    transition: text-decoration 0.2s ease;
  }

  .dropdown-menu a:hover {
    text-decoration: underline;
  }
  */



  /* Mobil header style */

/* DESKTOP DEFAULT: skjul mobil-elementer */
.hamburger,
.mobile-menu {
  display: none;
}

/* Media query: mobilvisning */
@media (min-width:768px){
  .mobile-menu{
    display: none;
  }
}

@media (max-width: 768px) {
  .nav-left,
  header > nav {
    display: none;
  }

  .logo {
    text-align: left;
    font-size: 1.5rem;
    text-align: left;
  }

  .hamburger {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    width: 24px;
    height: 18px;
    cursor: pointer;
    margin-left: auto;
  }

  .hamburger span {
    height: 2px;
    width: 100%;
    background-color: black;
  }

  .mobile-menu {
    display: none;
    flex-direction: column;
    background: white;
    position: absolute;
    top: 100%;
    right: 0;
    left: 0;
    padding: 1rem 2rem;
    gap: 1rem;
    z-index: 999;
    /* box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1); */
  }

  .mobile-menu a,
.submenu a,
.submenu-toggle {
  font-family: "futura-pt", sans-serif;
  font-size: 1.25rem;
  text-decoration: none;
  color: black;
}


  .mobile-menu.active {
    display: flex;
  }

  header {
    flex-wrap: wrap;
  }

  .center {
    flex: none;
    width: auto;
    margin-right:
  }

  /* Submenu styling */
  .submenu {
  display: none;
  flex-direction: column;
  gap: 0.5rem;
  padding-left: 1rem;
  /* padding-top: 0.5rem; */
}

.submenu.active {
  display: flex;
}

.submenu-toggle {
  cursor: pointer;
  font-weight: 200;
  /* text-decoration: underline; */
}

</style>

<script type="module">
  if (typeof window !== "undefined") {
    let lastScrollY = window.scrollY;
    const header = document.querySelector("header");

    window.addEventListener("scroll", () => {
      const currentScrollY = window.scrollY;

      if (currentScrollY > lastScrollY && currentScrollY > 100) {
        header.style.transform = "translateY(-100%)";
      } else {
        header.style.transform = "translateY(0)";
      }

      lastScrollY = currentScrollY;
    });
  }


  const hamburger = document.getElementById("hamburger");
  const mobileMenu = document.getElementById("mobileMenu");

  hamburger?.addEventListener("click", () => {
    mobileMenu?.classList.toggle("active");
  });

  const toggleDropdown = document.getElementById("toggleDropdown");
  const submenu = document.getElementById("submenu");

  toggleDropdown?.addEventListener("click", () => {
    submenu?.classList.toggle("active");
  });
</script>
